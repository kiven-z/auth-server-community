<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auth.service.system.message.mapper.InAppMessageUserStatusMapper">

    <!-- 公开消息已读状态懒写入：撞 uk 时补已读，已读时间不回写 -->
    <insert id="upsertReadBatch">
        INSERT INTO
        in_app_message_user_status (
        id,
        message_id,
        user_id,
        is_read,
        read_time,
        is_deleted,
        created_by,
        updated_by,
        version
        )
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (
            #{row.id},
            #{row.messageId},
            #{row.userId},
            #{row.isRead},
            #{row.readTime},
            #{row.isDeleted},
            #{row.createdBy},
            #{row.updatedBy},
            #{row.version}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        is_read = 1,
        read_time = IF(
        in_app_message_user_status.is_read = 1,
        in_app_message_user_status.read_time,
        VALUES
        (read_time)
        ),
        updated_by = VALUES(updated_by)
    </insert>

    <!-- 公开消息软删懒写入：撞 uk 时仅补删除，不回写已读 -->
    <insert id="upsertDeletedBatch">
        INSERT INTO
        in_app_message_user_status (
        id,
        message_id,
        user_id,
        is_read,
        read_time,
        is_deleted,
        created_by,
        updated_by,
        version
        )
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (
            #{row.id},
            #{row.messageId},
            #{row.userId},
            #{row.isRead},
            #{row.readTime},
            #{row.isDeleted},
            #{row.createdBy},
            #{row.updatedBy},
            #{row.version}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        is_deleted = 1,
        updated_by = VALUES(updated_by)
    </insert>

    <!-- 管理端：按任务分页查询读扩散互动用户 -->
    <select id="selectUserStatusPage"
            resultType="com.auth.service.system.message.model.po.InAppSendTaskRecipientPageRowPO">
        SELECT
        id,
        message_id,
        user_id,
        is_read,
        read_time,
        is_deleted,
        created_at,
        updated_at,
        created_by,
        updated_by
        FROM
        in_app_message_user_status
        <where>
            message_id = #{messageId}
            <if test="query.userId != null">
                AND user_id = #{query.userId}
            </if>
            <if test="query.isRead != null">
                AND is_read = #{query.isRead}
            </if>
            <if test="query.isDeleted != null">
                AND is_deleted = #{query.isDeleted}
            </if>
        </where>
        ORDER BY
        id DESC
    </select>

</mapper>
