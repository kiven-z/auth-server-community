<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auth.service.system.schedule.mapper.JobGroupMapper">

    <select id="selectListByPage" resultType="com.auth.service.system.schedule.model.po.JobGroupPageRowPO">
        SELECT
        id,
        group_code,
        group_name,
        description,
        status,
        is_system,
        order_num,
        created_at,
        updated_at,
        created_by,
        updated_by
        FROM job_group
        <where>
            <if test="query.groupCode != null and query.groupCode != ''">
                AND group_code LIKE CONCAT('%',
                #{query.groupCode},
                '%'
                )
            </if>
            <if test="query.groupName != null and query.groupName != ''">
                AND group_name LIKE CONCAT('%',
                #{query.groupName},
                '%'
                )
            </if>
            <if test="query.status != null">
                AND status =
                #{query.status}
            </if>
        </where>
        ORDER BY order_num
    </select>

    <select id="selectEnabledOptions" resultType="com.auth.service.system.schedule.model.po.JobGroupPageRowPO">
        SELECT
        id,
        group_code,
        group_name,
        description,
        status,
        is_system,
        order_num,
        created_at,
        updated_at,
        created_by,
        updated_by
        FROM job_group
        WHERE status = 1
        <if test="keyword != null and keyword != ''">
            AND
            (group_code LIKE CONCAT('%', #{keyword}, '%') OR group_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY order_num
        LIMIT #{limit}
    </select>
</mapper>
