---
description: Service / ServiceImpl Javadoc 统一写法
globs: **/*Service.java,**/*ServiceImpl.java
alwaysApply: false
---
# Service 注释规范

## 硬规则

1. 摘要行、`@param` / `@return` / `@throws` 说明**末尾禁止** `。` 与英文 `.`
2. 只写业务意图，不写实现细节（不堆表名、调用链、Redis/Outbox 细节）
3. 摘要一行说完；约束放括号，例如 `获取角色详情（含禁用）`
4. **契约写在接口**；`ServiceImpl` 覆盖方法一律 `{@inheritDoc}`（私有/包内方法除外，须自写摘要）
5. 类注释保留 `@author Bunny`

## 类注释

| 类型 | 写法 | 示例 |
|------|------|------|
| 普通 CRUD | `{业务对象}服务` | `系统角色服务` |
| 读写拆分 | `{业务对象}{职责}服务` | `部门只读查询服务`、`部门写服务` |
| 编排/专项 | `{能力}：{要点}` | `HTTP 会话编排：登录/刷新/登出与 Refresh Cookie 写入` |
| 实现类 | 接口摘要 + `实现`（可插在冒号前） | `系统角色服务实现`、`会话管理实现：踢人…` |

去掉「应用服务」等多余后缀；接口与实现文案必须同源，禁止各写一套。

```java
/**
 * 系统角色服务
 *
 * @author Bunny
 */
```

```java
/**
 * 系统角色服务实现
 *
 * @author Bunny
 */
```

## 方法注释（接口）

动宾一句 + 可选括号约束；`@param` / `@return` 用名词短语。

| 场景 | 统一用 |
|------|--------|
| 分页 | `分页查询{对象}` |
| 详情 | `获取{对象}详情` |
| 列表/下拉 | `查询{对象}…` |
| 新增 | `新增` / `批量新增` |
| 更新 | `更新` |
| 启停 | `批量启停` |
| 删除 | `删除` / `批量删除`（逻辑/物理放括号） |
| 覆盖关联 | `全量覆盖…` / `覆盖写入…` |

```java
/**
 * 分页查询角色
 * @param query 查询条件
 * @return 分页数据
 */
```

```java
/**
 * {@inheritDoc}
 */
@Override
public PageResponse<SysRolePageVO> getPage(SysRoleQuery query) { ... }
```

## 禁止

- 末尾句号；单独写 `详情` / `分页查询` 等过短摘要
- Impl 重复粘贴接口业务说明
- 「为了兼容」「与…对齐」「综上所述」「具有重要意义」「根据规定」等套话
